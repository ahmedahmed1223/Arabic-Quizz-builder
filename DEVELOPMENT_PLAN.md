# خطة تطوير منصة المسابقات التفاعلية (Arabic Quiz Builder)

> **المستودع**: [github.com/ahmedahmed1223/Arabic-Quizz-builder](https://github.com/ahmedahmed1223/Arabic-Quizz-builder)
> **الإصدار الحالي**: V15.2  ·  **الإصدار المستهدف**: V15.0
> **تاريخ الخطة**: أغسطس 2026  ·  **المدة**: 24 أسبوعاً (6 أشهر)
> **الجمهور**: مديرو المشروع وفريق التطوير
> **الأساس**: تحليل ثابت فعلي للكود (worklog.md، 598 سطراً)

---

## 📑 فهرس المحتويات

1. [الملخص التنفيذي](#1-الملخص-التنفيذي)
2. [تحليل الوضع الحالي](#2-تحليل-الوضع-الحالي)
3. [تحليل الأخطاء المكتشفة](#3-تحليل-الأخطاء-المكتشفة)
4. [تحليل SWOT والأهداف الاستراتيجية](#4-تحليل-swot-والأهداف-الاستراتيجية)
5. [خارطة الطريق المرحلية](#5-خارطة-الطريق-المرحلية)
6. [جدول المهام الكامل](#6-جدول-المهام-الكامل)
7. [تحليل المخاطر](#7-تحليل-المخاطر-وخطط-التخفيف)
8. [مؤشرات الأداء KPIs](#8-مؤشرات-الأداء-kpis)
9. [معايير الكود و CI/CD](#9-معايير-الكود-و-cicd)
10. [الخاتمة والتوصيات](#10-الخاتمة-والتوصيات)

---

## 1. الملخص التنفيذي

يقدم هذا المستند خطة تطوير شاملة لمستودع Arabic-Quizz-builder، وهو منصة مسابقات تفاعلية عربية تعمل بالكامل دون اتصال بالإنترنت ومبنية باستخدام Vite 5.x مع دعم أندرويد عبر Capacitor 7.6. المشروع بلغ الإصدار V15.2 ويتضمن 44 وحدة جافاسكربت، 12 طبقة تحسين CSS، 7 مكتبات طرف ثالث، و 3303 سؤال موزعة على 64 قسماً، بحجم بناء نهائي يقارب 7.3 ميجابايت في ملف HTML واحد مستقل.

أظهر التحليل الثابت للكود (Static Analysis) الذي وثّق في ملف `worklog.md` وجود **12 خطأً حرجاً** من المستوى P0 و P1، إضافة إلى **15 مشكلة أداء** متقدمة تؤثر مباشرة على تجربة المستخدم. أبرز هذه الأخطاء: دالة `exportQuiz` غير معرّفة تتسبب في فشل زر التصدير، خاصية `applyTheme` تشير إلى سمات غير موجودة، خطأ في أسماء خصائص الحالة داخل ملف `freeze-fix` يجعل آلية الاستعادة من التجمد غير فعّالة، وأداء منخفض في IndexedDB بسبب عمليات تسلسلية يمكن دمجها في معاملة واحدة.

تقترح الخطة **أربعة مراحل متتابعة على مدى 24 أسبوعاً**: المرحلة الأولى (أسابيع 1-4) تركّز على إصلاح الأخطاء الحرجة P0/P1 لاستعادة الاستقرار الأساسي؛ المرحلة الثانية (أسابيع 5-10) تتناول إعادة الهيكلة وإدخال الاختبارات الآلية ونظام CI/CD؛ المرحلة الثالثة (أسابيع 11-16) مخصصة لتحسينات الأداء شاملةً معالجة IndexedDB والتمرير الافتراضي وتقسيم الحزمة؛ والمرحلة الرابعة (أسابيع 17-24) توسّع المنصة بميزات جديدة تشمل دعم iOS، تحسينات PWA، وتحليلات تعليمية متقدمة.

تشمل النتائج المتوقعة: تقليل حجم الحزمة بنسبة لا تقل عن 30%، رفع Lighthouse Score إلى ما يفوق 90، تقليل زمن التحميل الأولي إلى أقل من ثانيتين على أجهزة أندرويد متوسطة، توفير تغطية اختبارات لا تقل عن 70% للمكونات الحرجة، والقضاء على جميع أخطاء P0/P1 المُكتشفة.

---

## 2. تحليل الوضع الحالي

### 2.1 نظرة عامة على المشروع

منصة المسابقات التفاعلية (Arabic Quiz Builder) هي تطبيق ويب عربي بالكامل لإنشاء واستضافة مسابقات تعليمية وترفيهية. يعمل التطبيق دون اتصال بالإنترنت بعد التحميل الأولي، ويُبنى كملف HTML واحد مستقل للسهولة في التوزيع. يدعم المشروع ثلاثة أنماط لعب رئيسية: مسابقات الفرق، الوضع الفردي، ووضع التدريب؛ وعشرة أنواع أسئلة مختلفة تشمل النصي، صح/خطأ، أكمل الفراغ، الترتيب، التوصيل، الرياضيات مع KaTeX، حفظ القرآن، الصور، الصوت، والفيديو.

بدأ المشروع كملف HTML واحد ضخم بلغ 6.4 ميجابايت و 40,139 سطراً، ثم أعيدت هيكلته كمشروع Vite نظيف متعدد الملفات مع الحفاظ على نموذج البناء كملف واحد للنشر. يدعم التطبيق وضع العرض التقديمي عبر البروجكتر مع شاشة تحكم عن بُعد وشاشة جمهور منفصلة، ويوفر نظام تكامل كامل مع تطبيقات أندرويد عبر Capacitor مع بناء APK جاهز للتوزيع المباشر من مجلد `releases/`.

### 2.2 الإحصائيات الحالية للمشروع

| المقياس | القيمة الحالية | ملاحظات |
|---|---|---|
| الإصدار | V15.2 | آخر تحديث: 23 يونيو 2026 |
| وحدات JS للتطبيق | 44 وحدة | في `src/app/` بترقيم متسلسل |
| طبقات تحسين CSS | 12 طبقة | تتضمن مكتبات طرف ثالث |
| مكتبات الطرف الثالث | 7 مكتبات | KaTeX، XLSX، Quill، Cropper، jsPDF، SortableJS، LZ-String |
| الأقسام المدمجة | 20 قسماً | 318 سؤالاً مدمجاً |
| الأقسام الخارجية | 64 قسماً | 3303 سؤالاً خارجياً |
| أيقونات SVG | 159 أيقونة | مكتبة داخلية مستوحاة من Lucide |
| اللغات المدعومة | 2 لغة | العربية (RTL) + الإنجليزية (LTR) |
| السمات الجاهزة | 19 سمة | + محرر سمات مخصص |
| حجم ناتج البناء | 7.3 ميجابايت | ملف HTML واحد مستقل |
| حجم APK أندرويد | 7.3 ميجابايت | متوافق مع Android 6.0+ |
| نقاط التوقف المتجاوبة | 15 نقطة | من 400px إلى 1025px+ |
| التغطية الاختبارية | غير متوفرة | لا توجد اختبارات آلية |

### 2.3 البنية التقنية

يعتمد المشروع على بنية فريدة تجمع بين نموذج Vite متعدد الملفات في التطوير ونموذج ملف HTML واحد في الإنتاج. تتم هذه العملية عبر إضافة Vite مخصصة (`vite-plugin-inline-classic-assets.js`) تقوم بتضمين جميع الوسوم `<script src>` و `<link rel=stylesheet>` ككتل مضمّنة، مع استبدال علامة `<!-- __BODY_MARKER__ -->` بقالب body، وترميز base64 للسكربتات التي تحتوي على أحرف تحكم (XLSX، jsPDF) لتجنب أخطاء parse5.

تعتمد البنية على نمط IIFE (Immediately Invoked Function Expression) المشترك بين جميع وحدات `src/app/`، مما يتيح نطاقاً معجمياً مشتركاً بين الملفات دون الحاجة إلى نظام وحدات ES. هذا النمط تاريخي وفعّال لمتطلبات ملف واحد، لكنه يفرض قيوداً على إعادة الاستخدام والاختبار، ويتسبب في ترتيب تحميل صارم يعتمد على البادئة الرقمية للملفات (`00-icon-library.js` إلى `45-solo-enhancer.js`).

طبقة التخزين تستخدم IndexedDB للوسائط (الصور، الصوت، الفيديو) و LocalStorage للحالة والتفضيلات مع ضغط LZ-String. طبقة الأمان تعتمد على Web Crypto API لتجزئة كلمات المرور بـ SHA-256، مع مُنظّف HTML ومحتوى أمان Content Security Policy. المزامنة بين النوافذ (شاشة الجمهور + التحكم عن بُعد) تعتمد على BroadcastChannel API.

### 2.4 نقاط القوة والضعف الهيكلية

**نقاط القوة:**
- دعم عربي كامل (RTL) منذ التصميم الأساسي، مع تكامل صحيح للخطوط العربية (Cairo، Tajawal، Amiri، Noto Naskh Arabic).
- يعمل دون اتصال بالكامل بعد التحميل الأولي — لا اعتماد على شبكة خارجية أثناء التشغيل.
- إمكانية وصول ملتزمة بمعايير WCAG AA: تباين لوني، حلقات focus-visible، مناطق ARIA حية، دعم قارئ الشاشة، التنقل بلوحة المفاتيح، مرشحات عمى الألوان، شريط حجم الخط.
- بناء ملف واحد مستقل يبسط التوزيع ويتيح الاستخدام دون خادم.
- تطبيق أندرويد أصلي عبر Capacitor مع APK موقّع وجاهز للتثبيت.

**نقاط الضعف:**
- نمط IIFE المشترك يمنع الاستفادة من نظام الوحدات ES الحديث (ESM)، ويُصعّب إعادة الاستخدام والاختبار المعزول.
- غياب تام لأي اختبارات آلية (Unit/Integration/E2E) — جميع عمليات التحقق تتم يدوياً.
- 44 وحدة JS بأحجام متفاوتة، بعضها يتجاوز آلاف الأسطر، مما يُصعّب الصيانة والمراجعة.
- حجم بناء نهائي 7.3 ميجابايت في ملف HTML واحد — حجم كبير يؤثر على زمن التحميل الأولي.
- عدم وجود TypeScript أو نظام فحص أنواع.
- غياب CI/CD رسمي متتبَّع في المستودع.
- الاعتماد على مكتبات طرف ثالث مُضمَّنة يدوياً (وليس عبر npm) في `src/vendor/`.

---

## 3. تحليل الأخطاء المكتشفة

أنتج التحليل الثابت الشامل للكود الذي وثّق في ملف `worklog.md` (598 سطراً) قائمة مفصّلة من الأخطاء والمشاكل، مصنّفة حسب الخطورة إلى أربعة مستويات: **P0** (حرجة، تمنع وظائف أساسية)، **P1** (متوسطة، تؤثر على الاستقرار أو تجربة المستخدم)، **P2** (تجميلية أو ثانوية)، ومشاكل أداء (Performance).

### 3.1 الأخطاء الحرجة من المستوى P0

| # | الوصف | الملف | التأثير |
|---|---|---|---|
| P0-1 | دالة `exportQuiz` غير معرّفة: زر التصدير في لوحة الإدارة يستدعي دالة غير موجودة | `src/templates/body.html:548` | نقر زر التصدير يرمي ReferenceError ويمنع تصدير بيانات المسابقة |
| P0-2 | أسماء خصائص حالة خاطئة في `freeze-fix`: يستخدم `state.currentQIdx` و `state.currentTeamIdx` بدلاً من `currentQIndex` و `currentTeamIndex` | `src/app/42-freeze-fix.js:107-108` | آلية استعادة الشاشة المتجمدة لا تعمل أبداً — الشرط دائماً false |
| P0-3 | `applyTheme('default')` و `applyTheme('highcontrast')` تشير إلى سمات غير موجودة في `THEMES[]` | `src/app/13-scoring.js:53,57` + `41-final-enhancements.js:883` | يُحفظ معرّف سمات غير صالح في الحالة، لا تطبّق تجاوزات CSS للسمات الفاتحة أو عالية التباين |
| P0-4 | `MediaDB.saveAllMedia/loadAllMedia` تُنفّذ N معاملات IndexedDB تسلسلية بدلاً من معاملة واحدة دفعية | `src/app/02-storage.js` | حفظ/تحميل 200 عنصر وسائط يستغرق 5-10 ثوانٍ على أندرويد متوسط، مع تجميد UI كامل |
| P0-5 | dirty-check لا يتخطى `MediaDB.saveAllMedia` عند عدم تغيّر الوسائط | `src/app/10-category-mgmt.js:249-253` | كل دورة save (حتى لتغييرات غير وسائطية) تشغّل حلقة IDB كاملة |
| P0-6 | `renderAdmin` يعيد بناء `innerHTML` كامل لجميع اللوحات في كل استدعاء | `src/app/16-encryption.js:235-240` | التنقل إلى لوحة الإدارة يجمّد UI لـ 50-300 ميلي ثانية على المسابقات الغنية بالوسائط |
| P0-7 | `VirtualList` و lazy-tabs في `38-periodic-cleanup.js` كود ميت غير مُوصَّل | `src/app/38-periodic-cleanup.js:7-118` | قسم به 200 سؤال يبني 200 عقدة DOM كاملة بدلاً من 10 مرئية فقط |
| P0-8 | `adjustScore(±1)` يُطلق إعادة بناء كاملة لقائمة الفرق + ticker + saveState عند كل نقرة | `src/app/16-encryption.js:1248` | النقر المتتالي على +1/-1 يسبب تقطّع بصري ومرورين كاملين للتخطيط لكل نقرة |

### 3.2 الأخطاء المتوسطة من المستوى P1

| # | الوصف | الملف | التأثير |
|---|---|---|---|
| P1-1 | `applyTheme` يضع علامة `manually_set` دائماً، حتى من الاستدعاءات التلقائية | `src/app/13-scoring.js:5,12` | بعد الكشف التلقائي الأول، لا يتبع التطبيق تغييرات الوضع الداكن/الفاتح للنظام |
| P1-2 | `loadState` يستدعي `saveState` مكشوفاً (دون try/catch) داخل `.then()` | `src/app/11-play-logic.js:519` | أي خطأ في saveState المعلّق يُبتلع بصمت، يفقد المستخدم تعديلاته دون إشعار |
| P1-3 | `initSystemThemeListener` يستدعي `applyThemeSmoothly` بسمة `'default'` غير الموجودة | `src/app/41-final-enhancements.js:883,895,905` | تغيير سمة النظام لا يحدّث متغيرات CSS للتطبيق |
| P1-4 | wrapper لـ `window.fetchExternalLibrary` يتعطل إذا الأصلية undefined | `src/app/41-final-enhancements.js:269-285` | نقر زر Fetch في واجهة استيراد المكتبة الخارجية يرمي TypeError |
| P1-5 | History-API hash navigation كود ميت — `window.init` غير معرّفة في النافذة الرئيسية | `src/app/39-history-aria.js:26-35` | التنقل المباشر عبر URL hash معطّل — التطبيق يبدأ دائماً من تسجيل الدخول |
| P1-6 | `_buildRemoteState` يقرأ `state.settings.competitionName` غير الموجود (الاسم الصحيح: `state.settings.name`) | `src/app/36-windows-templates.js:144` | اسم المسابقة فارغ دائماً في حمولة شاشة الجمهور والتحكم عن بُعد |
| P1-7 | `showSessionHistory` و `saveSessionHistory` يستخدمان `JSON.parse` مكشوفاً على localStorage | `src/app/17-history.js:116,122` | تلف بسيط في السلسلة المخزّنة يسبب SyntaxError غير معالَج |
| P1-8 | `_saveSoloProgress` يعيد رمي الأخطاء غير المتعلقة بالحصص دون catch في المستدعي | `src/app/20-display-modes.js:1037-1071` | أي خطأ saveState يُجمّد الوضع الفردي أثناء انتقال المستوى |
| P1-9 | مرجع `DEFAULT_BG_MUSIC` دون حارس `typeof` في `_saveStateNow` | `src/app/10-category-mgmt.js:222` | إذا فشل تحميل `05-audio-assets.js`، تتعطل طبقة الحفظ بالكامل |
| P1-10 | `TimerRegistry.register` مُشار إليها 3 مرات لكنها غير موجودة في الواجهة | `src/app/07-state-mgmt.js` + `21-solo-mode.js` + `30-more-i18n.js` | مؤقتات الوضع الفردي ومدة الجلسة تتسرب بصمت عبر تغييرات العرض |
| P1-11 | `TimerRegistry.clearAll` يعمل فقط على `beforeunload` — التعليق يَعِد بتنظيف عند تغيير العرض لكنه غير مُوصَّل | `src/app/01-foundation.js:265-266` | المؤقتات القديمة تستمر في العمل بعد مغادرة العرض، استنزاف CPU وبطارية |
| P1-12 | `42-freeze-fix.js` يسجل `setInterval` صحي (30 ثانية) دون تنظيف | `src/app/42-freeze-fix.js:172-197` | مؤقت يتيم ينجو من clearAll ويستمر بفحص عناصر DOM كل 30 ثانية |
| P1-13 | `HomeButtonFix` يلتف على `showView` بـ `setTimeout(fixHomeButtons, 100)` في كل تنقل | `src/app/42-freeze-fix.js:250-257` | 5-20 ميلي ثانية من مسح DOM لكل تبديل عرض، قابل للتكدّس عند التنقل السريع |
| P1-14 | `loadState` مع مهلة 8 ثوانٍ لـ `MediaDB.loadAllMedia` تتسابق مع `renderAdmin` | `src/app/11-play-logic.js:412-523` | صور تظهر متأخرة ثم تعيد الترتيب فجأة |
| P1-15 | `cleanupMemory` يستدعي `saveState()` بعد القص — يعيد دخول حلقة الحفظ المكلفة | `src/app/38-periodic-cleanup.js:267-301` | كل 5 دقائق أثناء الجلسة، عاصفة حفظ متزامنة 500ms-5s |

### 3.3 مشاكل الأداء المتقدمة

بالإضافة إلى الأخطاء المُصنّفة، حدّد التحليل 15 مشكلة أداء هيكلية تتراكم تأثيراتها على تجربة المستخدم طويلة المدى. أبرزها: تكرار `querySelector` على كل حدث تمرير، إعادة تسجيل مستمعات اللمس N×4 في كل render دون إزالة، وعكس payload الحالة الكامل إلى LocalStorage و sessionStorage المتزامنين في كل دفعة broadcast.

### 3.4 الأخطاء التجميلية من المستوى P2

هذه الأخطاء لا تؤثر على الوظائف لكنها تضر بالانطباع البصري العام. تشمل: نص `confirmAction` قد يعرض `'undefined'` على زر التأكيد عند نقص مفتاح i18n، صورة الخيار تحمل `alt=""` فارغاً مما يُخفيها عن قارئ الشاشة، تخطي هرمية العناوين من h1 إلى h3 في نافذة قص الصورة، وتعايش عنصري `role="main"` متداخلين في صفحة تسجيل الدخول.

---

## 4. تحليل SWOT والأهداف الاستراتيجية

### 4.1 تحليل SWOT الشامل

| المحور | النقاط الرئيسية |
|---|---|
| **نقاط القوة (Strengths)** | • تطبيق عربي أصيل بدعم RTL كامل<br>• يعمل دون اتصال بالكامل — ميزة تنافسية قوية<br>• إمكانية وصول ملتزمة بـ WCAG AA<br>• بناء ملف واحد يبسط التوزيع<br>• مكتبة أسئلة ضخمة (3303 سؤال)<br>• تطبيق أندرويد أصلي جاهز للتوزيع<br>• نظام تدويل ثنائي اللغة<br>• 19 سمة جاهزة + محرر مخصص |
| **نقاط الضعف (Weaknesses)** | • نمط IIFE المشترك يمنع ESM والاختبار المعزول<br>• غياب تام للاختبارات الآلية<br>• 8 أخطاء P0 حرجة غير مُعالَجة<br>• 15 خطأ P1 يؤثر على الاستقرار<br>• حجم بناء 7.3 ميجابايت كبير<br>• عدم وجود TypeScript أو فحص أنواع<br>• مكتبات طرف ثالث مُضمَّنة يدوياً<br>• غياب CI/CD رسمي متتبَّع<br>• ملفات JS ضخمة (بعضها آلاف الأسطر) |
| **الفرص (Opportunities)** | • توسعة إلى iOS عبر Capacitor (البنية جاهزة)<br>• وضع تعدد اللاعبين عبر الشبكة (WebRTC/WebSocket)<br>• تحليلات تعليمية وتقارير للمعلمين<br>• تكامل مع LMS (Moodle، Google Classroom)<br>• سوق عربي تعليمي متنامٍ يفتقر لأدوات عربية<br>• تحويل إلى PWA قابل للتثبيت<br>• إضافة AI لتوليد الأسئلة تلقائياً<br>• دعم لغات إضافية (فرنسي، أوردي) |
| **التهديدات (Threats)** | • منافسون دوليون (Kahoot، Quizizz) يدعمون العربية جزئياً<br>• تغييرات سياسات المتصفحات قد تكسر BroadcastChannel<br>• قيود تخزين IDB على Safari قد تحدّ النمو<br>• ثغرات أمنية في مكتبات قديمة (Quill، Cropper)<br>• اعتماد المستخدمين على المكتبة الخارجية عبر GitHub raw<br>• تآكل جودة الكود مع نمو الفريق دون معايير<br>• إهمال الصيانة قد يؤدي لتقادم سريع<br>• قيود bandwidth في المؤسسات التعليمية تُصعّب توزيع 7MB |

### 4.2 الأهداف الاستراتيجية SMART

#### الهدف 1: استعادة الاستقرار الأساسي للكود
معالجة 100% من أخطاء P0 (8 أخطاء) خلال الأسابيع الأربعة الأولى، و 80% من أخطاء P1 (12 من 15) بحلول نهاية الأسبوع الثامن. معيار القياس: عدد الأخطاء المفتوحة في تتبّع المشاكل = صفر لأخطاء P0، مع تقرير شهري يوثّق الإصلاحات. الهدف قابل للتحقيق لأن الإصلاحات محددة ومُوثَّقة في `worklog.md` مع حلول مقترحة لكل خطأ.

#### الهدف 2: تحسين الأداء بشكل قابل للقياس
تقليل حجم الحزمة النهائية بنسبة 30% (من 7.3 ميجابايت إلى أقل من 5.1 ميجابايت)، تقليل زمن التحميل الأولي على أندرويد متوسط إلى أقل من ثانيتين (مقابل 5-8 ثوانٍ حالياً)، ورفع Lighthouse Performance Score إلى ما يفوق 90. معيار القياس: تشغيل Lighthouse أسبوعياً عبر CI مع تسجيل النتائج. الهدف قابل للتحقيق عبر تقسيم الحزمة، معالجة IDB، والتمرير الافتراضي في المرحلة الثالثة.

#### الهدف 3: تحسين تجربة المطور (Developer Experience)
إدخال TypeScript تدريجياً (تبدأ بـ 30% من الملفات الجديدة)، تأسيس CI/CD عبر GitHub Actions مع ESLint صارم و Prettier، توفير تغطية اختبارات لا تقل عن 70% للمكونات الحرجة (storage، state، play-logic)، وتوثيق بنية المشروع في ملف `ARCHITECTURE.md`. معيار القياس: لوحة معلومات CI تعرض النتائج، تقرير تغطية كود أسبوعي.

#### الهدف 4: توسعة المنصة بميزات استراتيجية
إطلاق تطبيق iOS عبر Capacitor (البنية جاهزة)، إضافة وضع تعدد اللاعبين عبر الشبكة للاستخدام في الفصول الدراسية، تطوير لوحة تحليلات تعليمية للمعلمين تُظهر نقاط ضعف الطلاب، وتحويل التطبيق إلى PWA قابل للتثبيت. معيار القياس: إصدار V15.0 بتاريخ محدد في الأسبوع 24، مع beta test لـ 50 معلماً قبل الإطلاق العام.

---

## 5. خارطة الطريق المرحلية

تتألف الخطة من أربع مراحل متتابعة على مدى 24 أسبوعاً (6 أشهر). كل مرحلة لها هدف رئيسي واضح، مخرجات قابلة للقياس، ونقاط تفتيش (Checkpoints) لضمان الانتقال الآمن إلى المرحلة التالية. تم تصميم الترتيب بحيث تُعالج المشاكل الحرجة أولاً (لأنها تُعرقل كل شيء آخر)، ثم تُعاد هيكلة الكود (لتمكين الاختبارات والتحسينات)، ثم تُحسَّن الأداء (لأنه يتطلب كوداً مستقراً)، وأخيراً تُضاف الميزات الجديدة (على أساس متين).

### المرحلة 1: الإصلاح العاجل (أسابيع 1-4)
**الهدف**: معالجة جميع أخطاء P0 و 60% من أخطاء P1 المُكتشفة في `worklog.md` لاستعادة الاستقرار الأساسي للتطبيق. هذه المرحلة لا تتضمن إعادة هيكلة كبيرة — فقط إصلاحات نقطية محددة مع الحد الأدنى من التغيير في الكود المحيط.

### المرحلة 2: إعادة الهيكلة (أسابيع 5-10)
**الهدف**: إدخال البنية التحتية للمطورين — اختبارات آلية، CI/CD، TypeScript تدريجي، ESLint صارم. تبدأ هذه المرحلة بتأسيس الأدوات قبل إعادة هيكلة الكود الفعلية، لضمان وجود شبكة أمان قبل التحريك.

### المرحلة 3: تحسين الأداء (أسابيع 11-16)
**الهدف**: تحقيق الأهداف الكمية للأداء المُحدّدة في القسم السابق. تتطلب هذه المرحلة كوداً مستقراً (مضموناً بالمرحلة 1) واختبارات (مضمونة بالمرحلة 2) لأن تحسينات الأداء غالباً ما تُعيد هيكلة المسارات الحرجة.

### المرحلة 4: التوسعة والميزات الجديدة (أسابيع 17-24)
**الهدف**: إضافة قيمة استراتيجية للمنصة بعد استقرار الكود وتحسين الأداء. تشمل الميزات الجديدة ما يوسّع قاعدة المستخدمين (iOS، تعدد اللاعبين) وما يُعزّز القيمة التعليمية (تحليلات، تكامل LMS).

### الاعتماديات بين المراحل
المراحل مترابطة بشكل صارم: لا يمكن بدء المرحلة 2 قبل إغلاق 100% من أخطاء P0 (لأن الاختبارات ستفشل على كود غير مستقر). لا يمكن بدء المرحلة 3 قبل تأسيس CI/CD (لأن تحسينات الأداء تحتاج لقياسات آلية). لا يمكن بدء المرحلة 4 قبل استقرار الأداء (لأن الميزات الجديدة ستُضاف على أساس هش). أي تأخير في مرحلة يؤدي لتأخير مماثل أو مضاعف في المراحل اللاحقة.

---

## 6. جدول المهام الكامل

> جميع المهام قابلة للتتبع عبر checkboxes (`- [ ]`). كل مهمة لها: المعرّف، الأولوية (P0/P1/P2/P3)، المرحلة، المسؤول المقترح، الجهد بنقاط القصة (1 SP ≈ نصف يوم عمل)، ومعيار قبول مختصر.

### 6.1 المرحلة 1 — الإصلاح العاجل (أسابيع 1-4)

#### الأسبوع 1 — أخطاء P0 الأساسية

- [ ] **T-001** [P0] إصلاح `exportQuiz → exportData` في زر التصدير — `src/templates/body.html:548` — **المسؤول**: مطور أمامي — **الجهد**: 1 SP — **معيار القبول**: نقر الزر يُصدّر JSON
- [ ] **T-002** [P0] تصحيح أسماء خصائص الحالة في `freeze-fix` (`currentQIdx → currentQIndex`, `currentTeamIdx → currentTeamIndex`) — `src/app/42-freeze-fix.js:107-108` — **المسؤول**: مطور أمامي — **الجهد**: 1 SP — **معيار القبول**: استعادة الشاشة المتجمدة فعّالة
- [ ] **T-003** [P0] استبدال `'default'` بـ `'space'` و `'highcontrast'` بسمة فعلية في `applyTheme` — `src/app/13-scoring.js:53,57` — **المسؤول**: مطور أمامي — **الجهد**: 1 SP — **معيار القبول**: السمة الفاتحة وعالية التباين تتطبّقان بشكل صحيح

#### الأسبوع 2 — تحسينات التخزين

- [ ] **T-004** [P0] إعادة كتابة `MediaDB.saveAllMedia/loadAllMedia` لاستخدام معاملة IndexedDB واحدة دفعية — `src/app/02-storage.js` — **المسؤول**: مطور تخزين — **الجهد**: 5 SP — **معيار القبول**: 200 عنصر في < 500ms (كان 5-10 ثوانٍ)
- [ ] **T-005** [P0] إضافة `_mediaDirty` flag لتخطّي حفظ IDB عند عدم تغيّر الوسائط — `src/app/10-category-mgmt.js:249-253` — **المسؤول**: مطور تخزين — **الجهد**: 2 SP — **معيار القبول**: حفظ غير وسائطي لا يلمس IDB

#### الأسبوع 3 — تحسينات العرض والإدارة

- [ ] **T-006** [P0] تتبّع `_catDirty/_teamDirty/_qDirty` لكل لوحة + إعادة العرض الجزئي — `src/app/16-encryption.js:235-240` — **المسؤول**: مطور واجهة — **الجهد**: 5 SP — **معيار القبول**: renderAdmin يدوي لكل لوحة
- [ ] **T-007** [P0] توصيل `VirtualList` و `selectCatAdminEnhanced` (أو حذف الكود الميت) — `src/app/38-periodic-cleanup.js:7-118` — **المسؤول**: مطور واجهة — **الجهد**: 3 SP — **معيار القبول**: قائمة 200 سؤال = 10 عقد DOM مرئية
- [ ] **T-008** [P0] patch `adjustScore` لتحديث DOM فقط للفريق المتغيّر — `src/app/16-encryption.js:1248` — **المسؤول**: مطور واجهة — **الجهد**: 2 SP — **معيار القبول**: نقر متتالي بدون تقطّع

#### الأسبوع 4 — إصلاحات P1 الحرجة

- [ ] **T-009** [P1] إضافة `isManual` parameter لـ `applyTheme` — `src/app/13-scoring.js:5,12` — **المسؤول**: مطور أمامي — **الجهد**: 1 SP — **معيار القبول**: مستمع سمة النظام يبقى فعّالاً
- [ ] **T-010** [P1] إحاطة `saveState` في `loadState` بـ `try/catch` — `src/app/11-play-logic.js:519` — **المسؤول**: مطور أمامي — **الجهد**: 1 SP — **معيار القبول**: تسجيل تحذير بدل ابتلاع صامت
- [ ] **T-011** [P1] حارس `fetchExternalLibrary` wrapper ضد undefined — `src/app/41-final-enhancements.js:269-285` — **المسؤول**: مطور أمامي — **الجهد**: 1 SP — **معيار القبول**: toast بدل TypeError
- [ ] **T-012** [P1] ربط hash navigation بـ `_showInitialView` — `src/app/39-history-aria.js:26-35` — **المسؤول**: مطور أمامي — **الجهد**: 2 SP — **معيار القبول**: URL `#admin` يفتح لوحة الإدارة
- [ ] **T-013** [P1] تصحيح `competitionName → state.settings.name` — `src/app/36-windows-templates.js:144` — **المسؤول**: مطور نوافذ — **الجهد**: 1 SP — **معيار القبول**: اسم المسابقة يظهر في شاشة الجمهور
- [ ] **T-014** [P1] إحاطة `JSON.parse` في `history.js` بـ `try/catch` — `src/app/17-history.js:116,122` — **المسؤول**: مطور أمامي — **الجهد**: 1 SP — **معيار القبول**: سجل تالف لا يُسقط التطبيق

### 6.2 المرحلة 2 — إعادة الهيكلة (أسابيع 5-10)

#### الأسبوع 5-6 — تأسيس الاختبارات و CI/CD

- [ ] **T-015** [P1] تغليف `_saveSoloProgress` في `try/catch` أعلى — `src/app/20-display-modes.js:1037-1071` — **المسؤول**: مطور لعب — **الجهد**: 2 SP — **معيار القبول**: خطأ saveState لا يُجمّد الوضع الفردي
- [ ] **T-016** [P1] حارس `typeof DEFAULT_BG_MUSIC` في `_saveStateNow` — `src/app/10-category-mgmt.js:222` — **المسؤول**: مطور تخزين — **الجهد**: 1 SP — **معيار القبول**: فشل تحميل audio لا يكسر الحفظ
- [ ] **T-017** [P1] إضافة `TimerRegistry.register` أو ترحيل للمواقع الثلاثة — `src/app/07-state-mgmt.js` — **المسؤول**: مطور أساسي — **الجهد**: 3 SP — **معيار القبول**: `clearAll` يُنظّف كل المؤقتات
- [ ] **T-018** [P1] ربط `TimerRegistry.clearByContext` بـ `showView` — `src/app/01-foundation.js:265-266` — **المسؤول**: مطور أساسي — **الجهد**: 2 SP — **معيار القبول**: مؤقتات العرض القديم تُنظَّف
- [ ] **T-019** [P1] ترحيل `42-freeze-fix` health check إلى `TimerRegistry` — `src/app/42-freeze-fix.js:172-197` — **المسؤول**: مطور أساسي — **الجهد**: 1 SP — **معيار القبول**: لا يوجد مؤقت يتيم
- [ ] **T-020** [P1] استبدال `HomeButtonFix` بـ event delegation — `src/app/42-freeze-fix.js:250-257` — **المسؤول**: مطور واجهة — **الجهد**: 2 SP — **معيار القبول**: لا setTimeout في كل تنقل
- [ ] **T-021** [P1] إلغاء `loadAllMedia` عند انتهاء مهلة 8 ثوانٍ — `src/app/11-play-logic.js:412-523` — **المسؤول**: مطور تخزين — **الجهد**: 2 SP — **معيار القبول**: لا سباق بين timeout و render
- [ ] **T-022** [P1] `cleanupMemory` يستخدم `saveStateSync` بدل `saveState` — `src/app/38-periodic-cleanup.js:267-301` — **المسؤول**: مطور أداء — **الجهد**: 2 SP — **معيار القبول**: لا عاصفة IDB كل 5 دقائق
- [ ] **T-023** [P2] تثبيت Vitest + أول smoke test — **المسؤول**: مهندس اختبار — **الجهد**: 3 SP — **معيار القبول**: `npm test` ينجح
- [ ] **T-024** [P2] إعداد GitHub Actions workflow كامل (lint + test + build + APK) — **المسؤول**: مهندس DevOps — **الجهد**: 5 SP — **معيار القبول**: CI pipeline على كل PR
- [ ] **T-025** [P2] ESLint config صارم + Prettier + Husky pre-commit — **المسؤول**: مهندس DevOps — **الجهد**: 3 SP — **معيار القبول**: lint يفشل على الكود غير القياسي

#### الأسبوع 7-10 — ترحيل TypeScript والتوثيق

- [ ] **T-026** [P2] ترحيل 9 وحدات إلى TypeScript (storage، state، i18n) — **المسؤول**: مطور أمامي — **الجهد**: 8 SP — **معيار القبول**: 20% من الكود بـ TypeScript
- [ ] **T-027** [P2] تأسيس `ARCHITECTURE.md` + تحديث `README` و `CONTRIBUTING` — **المسؤول**: محرّر تقني — **الجهد**: 3 SP — **معيار القبول**: مطور جديد يفهم البنية في ساعة
- [ ] **T-028** [P2] إصلاح أخطاء P2 التجميلية (confirmAction undefined، alt نصوص صور الخيارات، هرمية العناوين، role=main متداخل) — **المسؤول**: مطور واجهة — **الجهد**: 3 SP — **معيار القبول**: تمرير فحص إمكانية الوصول

### 6.3 المرحلة 3 — تحسين الأداء (أسابيع 11-16)

- [ ] **T-029** [P2] Code splitting: تقسيم vendor/app/templates إلى chunks منفصلة — **المسؤول**: مهندس بناء — **الجهد**: 5 SP — **معيار القبول**: حزمة أولية < 2MB
- [ ] **T-030** [P2] Lazy loading للصور عبر `IntersectionObserver` في `renderQuestionsAdmin` — **المسؤول**: مطور واجهة — **الجهد**: 3 SP — **معيار القبول**: 10 عقد بدل 200 في قائمة كبيرة
- [ ] **T-031** [P2] Event delegation لـ `GestureManager.initLongPress` — **المسؤول**: مطور واجهة — **الجهد**: 3 SP — **معيار القبول**: مستمع واحد بدل N×4
- [ ] **T-032** [P2] إزالة LS/SS mirror في `_pushRemoteState` (الاعتماد على BroadcastChannel فقط) — `src/app/36-windows-templates.js:489-510` — **المسؤول**: مطور نوافذ — **الجهد**: 2 SP — **معيار القبول**: BroadcastChannel فقط
- [ ] **T-033** [P2] Cache `querySelector('.admin-content')` في scroll listener — `src/app/41-final-enhancements.js:930-945` — **المسؤول**: مطور أداء — **الجهد**: 1 SP — **معيار القبول**: لا querySelector على كل حدث تمرير
- [ ] **T-034** [P2] تقسيم CSS: إزالة الأنماط غير المستخدمة + Critical CSS inline — **المسؤول**: مطور واجهة — **الجهد**: 4 SP — **معيار القبول**: CSS < 200KB
- [ ] **T-035** [P2] تحسينات أندرويد: native splash screen، background fetch، تقليل حجم APK — **المسؤول**: مهندس موبايل — **الجهد**: 5 SP — **معيار القبول**: APK < 5MB، Lighthouse على Pixel 4a ≥ 90

### 6.4 المرحلة 4 — التوسعة والميزات الجديدة (أسابيع 17-24)

- [ ] **T-036** [P3] تكوين iOS في Capacitor + بناء IPA — **المسؤول**: مهندس موبايل — **الجهد**: 5 SP — **معيار القبول**: IPA مثبَّت على iPhone 12+
- [ ] **T-037** [P3] خادم WebSocket لوضع تعدد اللاعبين عبر الشبكة — **المسؤول**: مهندس خلفية — **الجهد**: 8 SP — **معيار القبول**: 10 أجهزة، تأخير < 200ms
- [ ] **T-038** [P3] لوحة تحليلات للمعلمين + تصدير PDF (نقاط ضعف الطلاب، إحصائيات) — **المسؤول**: مطور أمامي — **الجهد**: 8 SP — **معيار القبول**: 5 مقاييس مُتتبَّعة
- [ ] **T-039** [P3] تكامل LMS (Moodle، Google Classroom) — **المسؤول**: مهندس خلفية — **الجهد**: 6 SP — **معيار القبول**: استيراد أسئلة من Moodle
- [ ] **T-040** [P3] Service Worker متقدم + PWA manifest (تثبيت على سطح المكتب) — **المسؤول**: مهندس PWA — **الجهد**: 5 SP — **معيار القبول**: قابل للتثبيت على Chrome/Edge
- [ ] **T-041** [P3] AI لتوليد الأسئلة تلقائياً (تكامل مع LLM) — **المسؤول**: مهندس AI — **الجهد**: 8 SP — **معيار القبول**: توليد 10 أسئلة من موضوع
- [ ] **T-042** [P3] دعم لغات إضافية (فرنسي، أوردي) — **المسؤول**: مطور i18n — **الجهد**: 4 SP — **معيار القبول**: 4 لغات مدعومة
- [ ] **T-043** [P3] Beta test مع 50 معلماً + إصلاحات نهائية — **المسؤول**: مدير المنتج — **الجهد**: 5 SP — **معيار القبول**: ≥ 4/5 رضا، < 5% bug rate
- [ ] **T-044** [P3] إصدار V15.0 رسمي + تحديث CHANGELOG + GitHub Release — **المسؤول**: مدير الإصدار — **الجهد**: 2 SP — **معيار القبول**: tag `v15.0.0` على main

### 6.5 ملخص الجهد

| المرحلة | عدد المهام | إجمالي Story Points | المدة |
|---|---|---|---|
| 1 — الإصلاح العاجل | 14 | 27 SP | 4 أسابيع |
| 2 — إعادة الهيكلة | 14 | 36 SP | 6 أسابيع |
| 3 — تحسين الأداء | 7 | 23 SP | 6 أسابيع |
| 4 — التوسعة | 9 | 43 SP | 8 أسابيع |
| **الإجمالي** | **44** | **129 SP** | **24 أسبوعاً** |

> مع فريق من 3 مطورين بدوام كامل (مطور أمامي، مطور خلفية/تخزين، مهندس DevOps/اختبار)، يمكن إنجاز الخطة في 24 أسبوعاً مع هامش أمان 20% للمراجعات والاجتماعات.

---

## 7. تحليل المخاطر وخطط التخفيف

| الخطر | الاحتمال | التأثير | التصنيف | خطة التخفيف |
|---|---|---|---|---|
| فقدان بيانات المستخدمين أثناء إعادة هيكلة طبقة التخزين | متوسط | كارثي | **مرتفع** | إضافة migration script قبل أي تغيير في schema، نسخ احتياطي تلقائي قبل كل إصدار، اختبار الترقية على بيانات حقيقية قبل النشر |
| كسر التوافق مع الإصدارات السابقة في ملفات الحالة المخزّنة | عالي | عالي | **مرتفع** | إصدار `version` field في كل `saveState`، منطق migration تصاعدي، اختبارات توافق مع بيانات V14-V15 |
| تكدّس المهام وتأخير المراحل بسبب تقديرات متفائلة | عالي | متوسط | **مرتفع** | هامش أمان 20% في كل Sprint، مراجعة أسبوعية للسرعة (Velocity)، تقليص النطاق بدل تأخير الإصدار |
| أداء أندرويد منخفض رغم التحسينات (قيود الجهاز) | متوسط | عالي | **متوسط** | اختبار مبكر على Pixel 4a و Samsung A32، تخفيض الحد الأدنى للأداء إن لزم، Feature detection لتعطيل الميزات الثقيلة |
| ثغرات أمنية في مكتبات قديمة (Quill 1.x، Cropper 1.x) | متوسط | عالي | **متوسط** | ترقية Quill إلى 2.x في المرحلة 2، استبدال Cropper بـ alternatives حديثة، فحص `npm audit` أسبوعياً في CI |
| اعتماد المستخدمين على المكتبة الخارجية عبر GitHub raw | عالي | متوسط | **متوسط** | توفير CDN احتياطي، تخزين مؤقت محلي، رسالة واضحة عند فشل التحميل، توثيق طريقة الاستضافة الذاتية |
| مقاومة الفريق لتبني TypeScript و CI/CD | متوسط | متوسط | **متوسط** | تدريب الفريق قبل البدء، ترحيل تدريجي而非 شامل، إظهار القيمة في الأسابيع الأولى |
| قيود quota في LocalStorage (5MB) للمسابقات الكبيرة | متوسط | متوسط | **متوسط** | ترحيل البيانات الكبيرة إلى IndexedDB، ضغط أفضل، تنبيه المستخدم عند اقتراب الحد |
| CORS أو تغييرات في `raw.githubusercontent.com` | منخفض | عالي | **متوسط** | تخزين المكتبة الخارجية محلياً عند أول تحميل ناجح، CDN احتياطي، رسالة واضحة عند الفشل |
| كسر توافق المتصفحات القديمة (Safari 14، Firefox 88) | منخفض | متوسط | **منخفض** | Browserslist محدّث، polyfills للـ APIs الحرجة، اختبار دوري على BrowserStack |

---

## 8. مؤشرات الأداء KPIs

### 8.1 المؤشرات التقنية

| المؤشر | الأساس (V15.2) | الهدف (V15.0) | مصدر القياس |
|---|---|---|---|
| حجم الحزمة النهائية | 7.3 ميجابايت | < 5.1 ميجابايت | `vite build` + تحليل bundle |
| Lighthouse Performance (Desktop) | غير مُقاس | ≥ 90 | Lighthouse CI على كل PR |
| Lighthouse Performance (Mobile) | غير مُقاس | ≥ 80 | Lighthouse CI على Pixel 4a |
| زمن التحميل الأولي (FCP) | غير مُقاس | < 1.5 ثانية | WebPageTest على 3G |
| زمن التحميل التفاعلي (TTI) | غير مُقاس | < 2.5 ثانية | WebPageTest على 3G |
| تغطية الاختبارات (Critical) | 0% | ≥ 70% | Vitest coverage report |
| أخطاء P0 المفتوحة | 8 | 0 | GitHub Issues |
| أخطاء P1 المفتوحة | 15 | ≤ 3 | GitHub Issues |
| نسبة TypeScript | 0% | ≥ 30% | `tsc --noEmit` + grep |
| حجم APK أندرويد | 7.3 ميجابايت | < 5 ميجابايت | gradle build output |

### 8.2 المؤشرات المنتجة

| المؤشر | الأساس | الهدف | مصدر القياس |
|---|---|---|---|
| معدل الأعطال (Crash Rate) | غير مُقاس | < 0.5% | Sentry أو بديل |
| معدل إكمال المسابقات | غير مُقاس | ≥ 85% | Analytics مدمج |
| متوسط مدة الجلسة | غير مُقاس | ≥ 15 دقيقة | Analytics مدمج |
| رضا المستخدمين (Beta) | غير مُقاس | ≥ 4/5 | استبيان نهاية Beta test |
| عدد المستخدمين النشطين شهرياً | غير مُقاس | ≥ 1000 (في 6 أشهر) | Analytics مدمج |
| تقييم متجر التطبيقات | غير متوفر | ≥ 4.2/5 | Google Play Store |

### 8.3 مؤشرات العملية

| المؤشر | الأساس | الهدف | مصدر القياس |
|---|---|---|---|
| زمن دورة PR (من فتح إلى دمج) | غير مُقاس | < 48 ساعة | GitHub Insights |
| عدد PRs المدموجة أسبوعياً | غير مُقاس | ≥ 5 | GitHub Insights |
| تكرار النشر (Deployment Frequency) | شهري | أسبوعي | GitHub Releases |
| زمن البناء (CI) | غير مُقاس | < 5 دقائق | GitHub Actions logs |
| نكسات الإنتاج (Rollbacks) | غير مُقاس | < 1 شهرياً | سجل الإصدارات |
| نسبة الكود المُراجَع قبل الدمج | غير مُقاس | 100% | Branch protection rules |

---

## 9. معايير الكود و CI/CD

### 9.1 نموذج تفرّع Git (GitFlow المبسّط)

| الفرع | الغرض | الحماية |
|---|---|---|
| `main` | الإصدارات الإنتاجية فقط | موافقة 2 مراجعين + CI ناجح |
| `develop` | التكامل المستمر | CI ناجح |
| `feature/*` | إصلاحات وميزات جديدة | يُدمج في develop عبر PR |
| `release/*` | إعداد الإصدارات | يُدمج في main و develop |
| `hotfix/*` | إصلاحات الإنتاج العاجلة | يُدمج في main و develop مباشرة |

### 9.2 معايير Pull Request

كل PR يجب أن يستوفي المعايير التالية قبل الدمج:
- [ ] وصف واضح للمشكلة والحل مع ربط بـ GitHub Issue
- [ ] حجم لا يتجاوز 500 سطر (تقسيم PRs الأكبر)
- [ ] اختبارات للمكونات الجديدة أو المُعدَّلة (تغطية ≥ 70% للكود المُضاف)
- [ ] نجاح CI pipeline كامل (lint، test، build)
- [ ] موافقة مراجعَين اثنين على الأقل (أحدهما من الفريق الأساسي)
- [ ] تحديث التوثيق إذا لزم (README، CHANGELOG، ARCHITECTURE)

### 9.3 قواعد ESLint المخصصة

قواعد إلزامية:
- `no-undef` — منع المراجع غير المعرّفة (يكشف أخطاء مثل P0-1)
- `no-unused-vars` — منع المتغيرات غير المستخدمة
- `no-console` في الإنتاج (تُسمح فقط في develop)
- `eqeqeq` — مساواة صارمة
- `no-var` — force `let`/`const`
- `prefer-const`
- `no-implicit-coercion`
- `prefer-arrow-callback`
- `no-floating-decimal`
- `complexity` ≤ 15

### 9.4 تغطية الاختبارات

الحد الأدنى للتغطية:
- **70% للمكونات الحرجة**: storage، state-mgmt، play-logic، scoring، auth-security، encryption
- **50% لباقي الوحدات**

تُقاس التغطية عبر Vitest + c8، وتُعرض كتعليق على كل PR عبر Codecov. الاختبارات E2E عبر Playwright تغطّي المسارات الحرجة: تسجيل الدخول، إنشاء مسابقة، تشغيل مسابقة فريق، الوضع الفردي، التصدير، الاستيراد.

### 9.5 CI/CD Pipeline عبر GitHub Actions

**workflow رئيسي** (`.github/workflows/ci.yml`) — يعمل على كل PR و push:
1. تثبيت الاعتماديات (`npm ci`)
2. فحص lint (`eslint src/`)
3. تشغيل الاختبارات (`vitest run`)
4. البناء (`vite build`)
5. تحليل الحزمة (`bundlewatch`)
6. رفع تقرير التغطية

**workflow إضافي للإصدارات** (`.github/workflows/release.yml`) — يعمل عند tag جديد:
1. بناء APK موقّع
2. إنشاء GitHub Release مع الأصول
3. تحديث CHANGELOG.md تلقائياً

### 9.6 معايير Commit (Conventional Commits)

| النوع | الاستخدام |
|---|---|
| `feat:` | لميزة جديدة |
| `fix:` | لإصلاح خطأ |
| `refactor:` | لإعادة هيكلة |
| `perf:` | لتحسين أداء |
| `docs:` | للتوثيق |
| `test:` | للاختبارات |
| `chore:` | للمهام الثانوية |
| `ci:` | لتكوين CI |

**مثال**: `feat(storage): add IDB batching for saveAllMedia`

### 9.7 قائمة مراجعة الكود (Code Review Checklist)

- [ ] هل يحل الكود المشكلة المُحدَّدة في Issue؟ هل يُغطّيها بالكامل؟
- [ ] هل هناك اختبارات كافية؟ هل تُثبت أن الكود يعمل؟
- [ ] هل الكود قابل للقراءة؟ هل أسماء المتغيرات والدوال واضحة؟
- [ ] هل هناك أخطاء محتملة (race conditions، null/undefined، edge cases)؟
- [ ] هل الكود يتبع معايير ESLint؟ هل لا يوجد `console.log` في الإنتاج؟
- [ ] هل الأداء مقبول؟ لا `querySelector` في حلقة، لا `innerHTML` في hot path؟
- [ ] هل التوثيق محدّث (JSDoc للدوال العامة، README للميزات الجديدة)؟
- [ ] هل التغيير متوافق مع الإصدارات السابقة؟ أو هل migration مُوثّق؟
- [ ] هل إمكانية الوصول محفوظة (aria-labels، keyboard nav، contrast)؟
- [ ] هل الأمان مُراعى (XSS، CSRF، input validation، no secrets in code)؟

---

## 10. الخاتمة والتوصيات

تقدّم هذه الخطة خارطة طريق واقعية وقابلة للتنفيذ لتطوير منصة المسابقات التفاعلية من V15.2 إلى V15.0 على مدى ستة أشهر. الخطة متجذّرة في تحليل ثابت فعلي للكود (وليس افتراضات نظرية)، مع **44 مهمة محددة**، 10 مخاطر مُحلَّلة، و 22 مؤشر أداء قابل للقياس. النجاح يعتمد على الالتزام بالمعالم الأسبوعية، الاستثمار المبكر في الاختبارات و CI/CD، والانضباط في مراجعة الكود.

### التوصيات الفورية للبدء

للأسبوع الأول من الخطة، نوصي بالخطوات التالية الفورية قبل أي كتابة كود:

1. **إنشاء GitHub Project board** مع الأعمدة: Backlog، In Progress، Review، Done — وإضافة جميع المهام الـ 44 من جدول المهام.
2. **تعيين مالك لكل مرحلة** (Phase Owner) مسؤول عن المعالم الأسبوعية.
3. **جدولة اجتماع تخطيط Sprint** أسبوعياً (يوم الأحد، 30 دقيقة) واجتماع مراجعة (يوم الخميس، 30 دقيقة).
4. **إنشاء قناة تواصل مخصصة** للخطة (Slack/Telegram) للمشاركة اليومية.
5. **تجهيز بيئة تطوير محلية** لكل مطور مع Node 18+ و Android Studio.
6. **البدء فوراً بالمهمة T-001** (إصلاح `exportQuiz`) كـ quick win لكسر الجمود وبناء الزخم.

### الرسالة الختامية

منصة المسابقات التفاعلية مشروع عربي قيّم ذو أساس تقني صلب رغم المشاكل الحالية. الإصلاحات المقترحة ليست إعادة كتابة من الصفر بل تحسينات موجّهة ومتدرّجة تحافظ على القيمة الموجودة وتُعزّزها. مع الالتزام بالخطة، يمكن للمشروع أن يصل إلى مستوى احترافي يُمكّنه من المنافسة على المستوى الإقليمي، ويُسهم في سدّ الفجوة في الأدوات التعليمية العربية عالية الجودة. الاستثمار في الاختبارات و CI/CD اليوم سيوفّر أضعاف وقته في المستقبل، ويُمكّن الفريق من التطوير بثقة وسرعة دون خوف من كسر ما يعمل.

---

## 📊 ملخص الإحصائيات

| المقياس | القيمة |
|---|---|
| العدد الإجمالي للمهام | 44 مهمة |
| المهام P0 (حرجة) | 8 |
| المهام P1 (متوسطة) | 14 |
| المهام P2 (ثانوية) | 13 |
| المهام P3 (توسعة) | 9 |
| إجمالي الجهد | 129 Story Points |
| المدة الإجمالية | 24 أسبوعاً (6 أشهر) |
| عدد المراحل | 4 |
| المخاطر المُحلَّلة | 10 |
| مؤشرات الأداء | 22 |

---

*آخر تحديث: أغسطس 2026 · الإصدار: V15.0-dev-plan · الحالة: جاهز للتنفيذ ✅*
