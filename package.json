{
  "name": "quiz-platform",
  "version": "15.0.0",
  "description": "منصة المسابقات التفاعلية V15.0 - Interactive Quiz Platform with Capacitor Android/iOS support",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "single-file": "vite build && echo '✓ Single-file build at dist/quiz-platform.html'",
    "cap:sync": "npm run build && npx cap sync android",
    "cap:copy": "npm run build && npx cap copy android",
    "cap:open": "npx cap open android",
    "android:build": "npm run build && npx cap sync android && npx cap open android",
    "android:release": "npm run build && npx cap sync android && cd android && ./gradlew assembleRelease",
    "android:debug": "npm run build && npx cap sync android && cd android && ./gradlew assembleDebug",
    "ios:sync": "npm run build && npx cap sync ios",
    "ios:open": "npx cap open ios",
    "ios:build": "npm run build && npx cap sync ios && npx cap open ios",
    "ios:debug": "npm run build && npx cap sync ios && npx cap run ios",
    "ios:release": "npm run build && npx cap sync ios && npx cap build ios --scheme=Release",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "format": "prettier --write \"src/**/*.{js,css,html}\" \"tests/**/*.{js,js}\"",
    "format:check": "prettier --check \"src/**/*.{js,css,html}\" \"tests/**/*.{js,js}\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui",
    "server:start": "cd server && node server.js",
    "server:dev": "cd server && node --watch server.js",
    "prepare": "husky install"
  },
  "keywords": [
    "quiz",
    "education",
    "arabic",
    "interactive",
    "katex",
    "quill",
    "vite",
    "single-file",
    "pwa",
    "capacitor",
    "ios",
    "android"
  ],
  "author": "Quiz Platform Team",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@vitest/ui": "^2.1.8",
    "eslint": "^9.39.4",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.1.1",
    "husky": "^9.1.7",
    "jsdom": "^25.0.1",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3",
    "terser": "^5.50.0",
    "vite": "^5.4.10",
    "vite-plugin-singlefile": "^2.0.3",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.7",
    "@capacitor/app": "^7.1.2",
    "@capacitor/cli": "^7.6.7",
    "@capacitor/core": "^7.6.7",
    "@capacitor/haptics": "^7.0.5",
    "@capacitor/ios": "^7.6.7",
    "@capacitor/keyboard": "^7.0.6",
    "@capacitor/splash-screen": "^7.0.5",
    "@capacitor/status-bar": "^7.0.6"
  },
  "lint-staged": {
    "src/**/*.{js,cjs,mjs}": [
      "eslint --max-warnings=0 --fix",
      "prettier --write"
    ],
    "src/**/*.{css,html,json}": [
      "prettier --write"
    ]
  }
}
